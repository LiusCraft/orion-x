# Optional: explicit API base URL. Leave empty to use dev proxy.
VITE_MANAGER_API_BASE_URL=

# Optional: Vite dev proxy target for /api and /internal.
VITE_MANAGER_API_PROXY_TARGET=http://127.0.0.1:8081
